#include <iostream>
#include <cmath>

using namespace std;

int main() {
	int N;
	cin >> N;
	*/
	long long n = 987654321;
	int count = 0;
	for (long long i = pow(10, 2*N - 10); i < pow(10, 2*N - 9); ++i) {
		long long c = n + i * 1000000000;
		if (c / (long long)sqrt(c) == sqrt(c)) {
			if (sqrt(c) / pow(10, N) == 0) {
				++count;
				cout << i << " " << (long long)sqrt(c) << endl;
			}
		}
	}
	*/
	cout << 0 << endl;
	return 0;
}
